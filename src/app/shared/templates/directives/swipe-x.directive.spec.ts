import { SwipeXDirective } from './swipe-x.directive';

describe('SwipeXDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeXDirective();
    expect(directive).toBeTruthy();
  });
});
