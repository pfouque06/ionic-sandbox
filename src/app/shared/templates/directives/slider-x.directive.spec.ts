import { SliderXDirective } from './slider-x.directive';

describe('SliderXDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderXDirective();
    expect(directive).toBeTruthy();
  });
});
